@model IEnumerable<AssetOverviewViewModel>

@{
    ViewData["Title"] = "View";
}

<h1>View</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FileName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreatedOn)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.FileName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CreatedOn)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.AssetId">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.AssetId">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.AssetId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
<div>Pagination partial view would go here...</div>

